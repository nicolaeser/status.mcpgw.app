services:
  mcp:
    image: ghcr.io/nicolaeser/status.mcpgw.app:latest
    container_name: status-mcpgw-app
    depends_on:
      - redis
    ports:
      - "3000:3000"
    environment:
      PORT: "3000"
      PUBLIC_BASE_URL: "http://localhost:3000"
      SERVER_NAME: "status.mcpgw.app"
      SESSION_TTL_MS: "1800000"
      AUTH_MODE: "bearer"
      API_KEY: "change-me-in-production"
      LOGGING_MODE: "none"
      REDIS_URL: "redis://redis:6379"
      RATE_LIMIT_WINDOW_MS: "60000"
      RATE_LIMIT_CLIENT_ID_HEADER: "x-client-id"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/internal-api/heartbeat"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

  redis:
    image: redis:7-alpine
    container_name: status-mcpgw-app-redis
    restart: unless-stopped
